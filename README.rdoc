== Pugaipadam

The photo wall for organizations

=== Installation

You need JRuby as there is a dependency on Oracle. However, if the dependency is removed you can use MRI

Clone the repository and run `bundle install`

Make sure you have mongodb server running in local as the employee details are stores as documents.

